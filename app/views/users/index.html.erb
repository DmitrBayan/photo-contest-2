<% provide(:title, 'All users') %>
<h1>All users</h1>
<%= will_paginate %>
<ul class="users">
    <%= render @users %>
</ul>
<% if !@users.present? %>
  <h2>Something strange... There are no users. <br> <br>
   Become a first, <%= link_to "LOG IN NOW", root_path %>!</h2>
<% end %>
<%= will_paginate %>