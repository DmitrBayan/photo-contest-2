<% provide(:title, "All posts") %>
<h1>All posts</h1>
<%= render "shared/filterable" %>
<%= render "shared/post_sort" %>

<% if @posts.blank? %>
	<p class="font-weight-bold">There are no posts, they may be on moderation.</p>
<% end %>

<%= render "shared/posts_index" %>
