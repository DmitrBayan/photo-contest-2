<% provide(:title, "All posts") %>
<h1>All posts</h1>
<%= render "shared/filterable" %>
<%= render "shared/post_sort" %>
<ul class="posts">
  <%= render @posts %>
  <% if !@posts.present? %>
    <h2>Nothing to show...</h2>
  <% end %>
</ul>
